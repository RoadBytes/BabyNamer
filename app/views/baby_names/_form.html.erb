<% if @baby_name.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@baby_name.errors.count, "error") %> prohibited this baby name from being saved:</h2>

    <ul>
    <% @baby_name.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
    </ul>
  </div>
<% end %>

<%= simple_form_for @baby_name do |f| %>
  <%= f.input :name %>
  <%= f.input :background, label: "Notes on this Name" %>
  <%= f.input :user_id, as: :hidden, input_html: { value: current_user.id } %>

  <%= f.button :submit, id: 'submit_new_baby_name' %>
<% end %>
